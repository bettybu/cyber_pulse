name: Update OSINT News Daily

on:
  schedule:
    # Uruchamia się codziennie o 6:00 rano czasu UTC (co odpowiada 8:00 czasu polskiego)
    - cron: '0 6 * * *'
  workflow_dispatch: # Pozwala też na uruchomienie bota ręcznie przyciskiem w GitHubie

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install feedparser requests

      - name: Run Python script to generate site
        run: |
          python make_site.py

      - name: Commit and push if it changed
        run: |
          git config --global user.name 'OSINT Bot'
          git config --global user.email 'bot@users.noreply.github.com'
          git add index.html
          git diff --quiet && git diff --cached --quiet || (git commit -m "Daily automated news update [skip ci]" && git push)